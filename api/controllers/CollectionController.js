/**
 * CollectionController
 *
 * @description :: Server-side logic for managing collections
 * @help        :: See http://links.sailsjs.org/docs/controllers
 */

module.exports = {

	// Override Blueprint action
	/*
	findOne : function (req, res, next) {
		var collectionId = req.param('id');// l'id è un parametro

		if (!collectionId) { return next(); }

		Collection.findOneById(collectionId)
	}

	User.count().where({follower: followerId})
		.exec(function(err, numberOfFollowings) {
  		//numberOfFollowings will be the integer that you need
	})
	*/
	
};

